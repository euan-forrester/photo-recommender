version: 0.1

phases:
  install:
    commands:
      - cd /frontend
      - yarn install
  build:
    commands:
      - yarn build --mode=production
  post_build:
    commands:
      - yarn deploy --mode=$ENVIRONMENT
