"type":"metric",
"properties": {
    "metrics": [
        [
            "AWS/ECS",
            "MemoryUtilization",
            "ClusterName",
            "${cluster_name}"
        ],
        [
            "AWS/ECS",
            "MemoryUtilization",
            "ClusterName",
            "${cluster_name}",
            "ServiceName",
            "puller-flickr-${environment}"
        ],
        [
            "AWS/ECS",
            "MemoryUtilization",
            "ClusterName",
            "${cluster_name}",
            "ServiceName",
            "ingester-database-${environment}"
        ],
        [
            "AWS/ECS",
            "MemoryUtilization",
            "ClusterName",
            "${cluster_name}",
            "ServiceName",
            "api-server-${environment}"
        ],
        [
            "AWS/ECS",
            "MemoryUtilization",
            "ClusterName",
            "${cluster_name}",
            "ServiceName",
            "scheduler-${environment}"
        ]
    ],
    "period":300,
    "stat":"Average",
    "region":"${region}",
    "title":"ECS Cluster ${cluster_name} Memory"
}