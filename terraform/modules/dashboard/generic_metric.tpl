"type":"metric",
"properties": {
    "metrics": [
        [
            "Photo Recommender",
            "${metric_name}",
            "Environment",
            "${environment}",
            "Process",
            "${process_name}"
        ]
    ],
    "period":300,
    "stat":"Average",
    "region":"${region}",
    "title":"${title}"
}